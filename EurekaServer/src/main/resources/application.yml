server:
  port: 8761  #当前Eureka Server服务端口

eureka:
  client:
    fetch-registry: false #是否获取其他Eureka Server 服务的数据
    register-with-eureka: false  #是否将当前的服务作为客户端进行注册
    service-url:
     defaultZone: http://localhost:8761/eureka/ #注册中心的访问地址